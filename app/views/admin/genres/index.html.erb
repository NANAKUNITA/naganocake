<div class="container">
    <div class="row">
        
        <h2>ジャンル一覧・追加</h2>
        
        <%= form_for (@genre, url: admin_genres_path) do |f| %> <!--form_forの引数には、モデルクラスのインスタンスを指定する。主にテーブルレコードを新規作成、更新するときに利用する。()は、なくても良い。https://pikawaka.com/rails/form_forを参照。-->
        <div class="field row"> <!-- field row????? -->
        <label>ジャンル名</label>
        <%= f.text_field :name %>
        <%= f.submit "新規登録", class: "btn btn-primary" %>
        </div>
        <% end %>
          <br>
          <!--　ジャンル一覧 -->
          <table class="table table-hover"><!-- table-hoverとは、マウスを乗せた際に背景色を変更すること。table class="table table-hover"と記述する。 -->
              <thead>
                  <tr class="active"><!-- activeクラスは、hoverで指定したことの背景色の種類。 -->
                      <th>ジャンル</th>
                      <th colspan="1"></th> <!-- tdまたはth colspan=""は、結合するセルの数が値になり、初期値は１。水平方向の結合を指定するときに使用。-->
                  </tr>
              </thead>
              <tbody>
                  <% @genres.each do |genre| %>
                  <tr>
                      <td><%= genre.name %></td>
                      <td><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "btn-btn-primary" %></td>
                  </tr>
                  <% end %>
              </tbody>
          </table>
          <%= paginate @genres %>
    </div>
</div>