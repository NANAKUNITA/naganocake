<div class="container">
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
        
        <h2>ジャンル一覧・追加</h2>
    <div class="form-group col-md-10">
        <%= form_with (model: @genre, url: admin_genre_path) do |f| %> <!--form_withの引数には、モデルクラスのインスタンスを指定する。主にテーブルレコードを新規作成、更新するときに利用する。()は、なくても良い。https://pikawaka.com/rails/form_forを参照。-->
        
        <%= f.label :ジャンル名 %>
        <%= f.text_field :name, class: "col-md-4" %><br>
        <%= f.submit "新規登録", class: "btn btn-primary" %>
        <% end %>
    </div>
          <br>
          <!--　ジャンル一覧 -->
          <table class="table genre-index"><!-- table-hoverとは、マウスを乗せた際に背景色を変更すること。table class="table table-hover"と記述する。 -->
              <thead>
                  <tr><!-- activeクラスは、hoverで指定したことの背景色の種類。 -->
                      <th>ジャンル</th>
                      <th colspan="1"></th> <!-- tdまたはth colspan=""は、結合するセルの数が値になり、初期値は１。水平方向の結合を指定するときに使用。-->
                  </tr>
              </thead>
              
              <tbody>
                  <% @genres.each do |genre| %>
                  <tr>
                      <td><%= genre.name %></td>
                      <td><%= link_to "編集する", edit_admin_genre_path(genre), class: "btn-btn-primary" %></td>
                  </tr>
                  <% end %>
              </tbody>
          </table>
          <%= paginate @genres %>
        </div>
    </div>
</div>