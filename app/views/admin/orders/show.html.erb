<div class="container my-5">
    <div class="row">
        <div class="col-xs-12">
            <h2>注文履歴一覧</h2>
            <!-- 購入詳細情報 -->
            <table>
                <tr>
                    <th class="order-show">購入者</th>
                    <td><%= @order.customer.last_name %><%= @order.customer.first_name %></td>
                </tr>
                <tr>
                    <!-- 注文個数の記入をする --><th class="order-show">注文個数</th>
                    <td></td>
                </tr>
            </table>
        </div>
    </div>
</div>