<div class="container my-5">
    <div class="row">
        <h2>商品一覧</h2>
    </div>
  <div class="row text-right">
    <%= link_to "+", new_admin_item_path, class: "btn btn-lg rounded-circle text-right ml-auto shadow-sm" %> <!-- link_toとは、リンクを作成するためのメソッド。Railsではlink_to、HTMLでは、aタグを使用する。基本的な構文は、%= link_to '表示したい文字', パス %。この基本的な構文に加え、:idがルートについている場合は、内部リンクのパスで（）のなかにパラメータを指定する必要がある。 -->
  </div>
  <div class="row mt-1">
     <table class="table table-hover text-center">
       <thead>
         <tr> 
           <th>商品ID</th>
           <th>商品名</th>
           <th>税抜価格</th>
           <th>ジャンル</th>
           <th>ステータス</th>
         </tr>
       </thead>

       <tbody>
         <% @items.each do |item| %>
         <tr>
           <td><%= item.id %></td> 
           <td><%= link_to item.name, admin_item_path(item) %></td>
           <td><%= item.price.to_s(:delimited, delimiter: ',') %></td>
           <td><%= item.genre.name %></td>
           <td><% if item.is_active == true %>
                 販売中
                 <% else %>
                 販売停止中
                 <% end %>
           </td>
         </tr>
         <% end %> 
       </tbody>
      </table>
   </div>
   <div class="container">
       <div class="row">
           <div class="col-md-4 offset-md-4 py-2 mt-5">
               <%= paginate @items %>
           </div>
       </div>
   </div>
</div>
